import Vue from 'vue'
// import store from '../store'

Vue.mixin({

  beforeCreate () {
    // store.commit('setGloblLoading', true)
  },
  mounted () {
    // store.commit('setGloblLoading', false)
  }
})
